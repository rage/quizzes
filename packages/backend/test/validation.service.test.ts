/*import ValidationService from "../src/services/validation.service"

import Database from "../src/config/database"

import { PeerReview, Quiz, QuizAnswer, UserQuizState } from "../src/models"

import data from "./data"

const validationService = new ValidationService()

test("doesn't change status for already confirmed answer", () => {
  const status = validationService.assessAnswerStatus(
    data.quiz as Quiz,
    data.quizAnswer as QuizAnswer,
    data.userQuizState as UserQuizState,
    data.passingPeerReviews as PeerReview[],
  )
  expect(status).toBe(data.quizAnswer.status)
})*/
