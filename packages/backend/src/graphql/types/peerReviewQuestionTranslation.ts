export const PeerReviewQuestionTranslation = `
  type PeerReviewQuestionTranslation {
    peerReviewQuestion: PeerReviewQuestion!,
    peerReviewQuestionId: String!,
    language: Language!,
    languageId: String!,
    title: String,
    body: String,
    createdAt: DateTime,
    updatedAt: DateTime
  }
`
