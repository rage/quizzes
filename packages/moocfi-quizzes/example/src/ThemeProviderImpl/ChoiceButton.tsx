import * as React from "react"
import styled from "styled-components"
import { ChoiceButtonProps } from "../../../src/components/ChoiceButton"

export default (props: ChoiceButtonProps) => {
  return <button {...props} />
  
}
